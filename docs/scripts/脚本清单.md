# Datus Agent 脚本清单

> **文档版本**: v1.4
> **更新日期**: 2026-01-28
> **相关目录**: `scripts/`

---

## 脚本分类概览

| 分类 | 脚本数量 | 说明 |
|------|----------|------|
| [Schema 元数据管理](#一schema-元数据管理) | 9 | Schema 迁移、诊断、验证、业务术语生成相关 |
| [模型下载](#二模型下载) | 2 | HuggingFace/ModelScope 模型下载 |
| [开发测试](#三开发测试) | 2 | 开发调试、测试相关 |
| [工具脚本](#四工具脚本) | 1 | 提示模板更新 |

---

## 一、Schema 元数据管理

> **相关文档**: [Schema 元数据管理脚本指南](../Schema%20元数据管理脚本指南.md)

### 1.1 migrate_v0_to_v1.py - Schema 迁移

**功能**: 将 LanceDB 中的 Schema 元数据从 v0 版本升级到 v1 增强版本。

**使用方式**:
```bash
python scripts/migrate_v0_to_v1.py --config=conf/agent.yml [选项]
```

**核心参数**:
| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--config` | 配置文件路径 | 必填 |
| `--namespace` | 命名空间 | 自动选择 |
| `--extract-statistics` | 收集统计信息 | false |
| `--extract-relationships` | 提取关系 | true |
| `--import-schemas` | 迁移后导入 Schema | false |
| `--clear` | 导入前清除数据 | false |
| `--force` | 强制重新迁移 | false |
| `--llm-enum-extraction` | LLM 增强枚举提取 | false |

**完整迁移示例**:
```bash
python scripts/migrate_v0_to_v1.py \
  --config=conf/agent.yml \
  --namespace=test \
  --extract-statistics=true \
  --extract-relationships=true \
  --import-schemas \
  --llm-enum-extraction \
  --clear \
  --force
```

---

### 1.2 diagnose_schema_ddl.py - DDL 抽样诊断

**功能**: 抽样检查 LanceDB 中存储的 DDL 是否存在缺逗号/截断等问题。

**使用方式**:
```bash
python scripts/diagnose_schema_ddl.py --config=conf/agent.yml --namespace=<name>
```

**核心参数**:
| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--config` | 配置文件路径 | conf/agent.yml |
| `--namespace` | 命名空间名称 | 自动选择 |
| `--catalog` | Catalog 过滤 | 空 |
| `--database` | Database 过滤 | 空 |
| `--limit` | 抽样表数量 | 5 |
| `--random` | 随机抽样 | false |
| `--compare-db` | 拉取数据库 DDL 对比 | false |

**示例**:
```bash
# 随机抽样 10 张表并对比数据库 DDL
python scripts/diagnose_schema_ddl.py \
  --config=conf/agent.yml \
  --namespace=test \
  --limit=10 \
  --random \
  --compare-db \
  --output=/tmp/ddl_sample.json
```

---

### 1.3 check_migration_report.py - 迁移质量报告

**功能**: 检查迁移覆盖率、字段填充率，生成质量报告。

**使用方式**:
```bash
python scripts/check_migration_report.py --config=conf/agent.yml [选项]
```

**参数**:
| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--config` | 配置文件路径 | 必填 |
| `--namespace` | 命名空间 | - |
| `--top-tags` | 显示标签数量 | 10 |

**报告内容**:
- v1 记录比例
- `table_comment` 填充率
- `column_comments` 填充率
- `column_enums` 填充率
- `business_tags` 填充率
- `relationship_metadata` 覆盖率

---

### 1.4 rebuild_schema.sh - 重建 Schema

**功能**: 清空现有 Schema 数据后重新从数据库导入。

**使用方式**:
```bash
bash scripts/rebuild_schema.sh --namespace=<name> [选项]
```

**参数**:
| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--namespace` | 命名空间名称 | 必填 |
| `--config` | 配置文件路径 | conf/agent.yml |
| `--clear` | 清除现有数据 | false |

**示例**:
```bash
# 标准重建
bash scripts/rebuild_schema.sh --namespace=my_database

# 清除数据后重建
bash scripts/rebuild_schema.sh --namespace=my_database --clear
```

---

### 1.5 backup_storage.sh - 备份存储

**功能**: 创建存储目录的带时间戳备份。

**使用方式**:
```bash
bash scripts/backup_storage.sh
```

**输出**:
```
检测到存储路径: /path/to/lancedb/storage
备份完成: /path/to/lancedb/storage.backup_v0_20260126_104800
```

---

### 1.6 live_bootstrap.py - 实时数据库引导

**功能**: 从实时数据库直接拉取 Schema 元数据，支持增量更新。

**使用方式**:
```bash
python scripts/live_bootstrap.py --config=conf/agent.yml [选项]
```

**核心参数**:
| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--config` | 配置文件路径 | 必填 |
| `--namespace` | 命名空间 | - |
| `--database` | 数据库名称过滤 | 空 |
| `--dialect` | SQL 方言 | duckdb |
| `--extract-statistics` | 收集统计信息 | false |
| `--extract-relationships` | 提取关系 | true |
| `--incremental` | 增量更新模式 | false |

**支持方言**: `duckdb`, `sqlite`, `mysql`, `starrocks`, `snowflake`

---

### 1.7 check_storage_path.py - 检查存储路径

**功能**: 验证 `agent.yml` 配置的存储路径是否正确。

**使用方式**:
```bash
python scripts/check_storage_path.py
```

---

### 1.8 local_init.py - Schema 导入（模块）

**功能**: 直接从数据库导入 Schema 元数据到 LanceDB。

**使用方式**:
```bash
python -m datus.storage.schema_metadata.local_init --config=conf/agent.yml [选项]
```

**核心参数**:
| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--config` | 配置文件路径 | 必填 |
| `--namespace` | 命名空间 | - |
| `--database` | 数据库名称 | 空 |
| `--llm-enum-extraction` | LLM 增强枚举提取 | false |

---

### 1.9 generate_business_config.py - 业务术语配置生成器

**功能**: 从数据架构设计文档和指标清单 Excel/CSV 生成业务术语配置文件 `business_terms.yml`，支持多行表头、表优先级过滤、文本清洗、LLM 增强和 DDL 注释合并。

**核心特性**:
- 支持 Excel (.xlsx) 和 CSV (.csv) 两种输入格式
- **表优先级过滤**: 优先使用 DIM/DWD/DWS 表，可选包含 ADS，排除 ODS
- **智能文本清洗**: 自动去除 emoji、序号标记、特殊符号，规范化空格
- 智能处理多行表头（数据架构3行表头、指标清单2行表头）
- 自动检测工作表（优先 "Sheet1"，否则第一个）
- **LLM 增强**: 术语消歧、指标定义改写、冲突解决
- 可合并现有 DDL 注释进行增强
- 生成三种映射关系：`term_to_table`、`term_to_schema`、`table_keywords`

**使用方式**:
```bash
python scripts/generate_business_config.py --config=conf/agent.yml --namespace=<name> [选项]
```

**核心参数**:
| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--config` | 配置文件路径 | 必填 |
| `--namespace` | 目标命名空间 | 必填 |
| `--arch-xlsx` | 数据架构详细设计 Excel 路径 | - |
| `--arch-csv` | 数据架构详细设计 CSV 路径（兼容） | - |
| `--arch-sheet-name` | 数据架构工作表名称/索引 | 自动检测 |
| `--metrics-xlsx` | 指标清单 Excel 路径 | - |
| `--metrics-csv` | 指标清单 CSV 路径（兼容） | - |
| `--metrics-sheet-name` | 指标清单工作表名称/索引 | 自动检测 |
| `--output` | 输出 YAML 文件路径 | `conf/business_terms.yml` |
| `--merge-ddl` | 合并现有 DDL 注释 | false |
| `--max-table-priority` | 最大表优先级（DIM/DWD/DWS/ADS/ODS） | ADS |
| `--disable-text-cleaning` | 禁用 Excel 文本清洗 | false |
| `--use-llm` | 启用 LLM 增强 | false |
| `--rewrite-with-llm` | LLM 改写指标定义（需配合 `--use-llm`） | false |
| `--include-confidence` | 输出置信度分数（调试用） | false |
| `--min-term-length` | 最小术语长度 | 2 |
| `-v, --verbose` | 详细输出（-v INFO, -vv DEBUG） | - |
| `--import-to-lancedb` | 导入指标到 ext_knowledge 表 | false |

**输入文件格式要求**:

数据架构详细设计 Excel（3行表头）:
- 第1行: 分组标题（资产目录/业务属性/技术属性/管理属性）
- 第2行: 实际列名（物理表名、字段名、分析对象等）⭐
- 第3行: 列说明/注释
- 关键列: `物理表名`、`字段名`、`属性（中文）`、`属性业务定义`、`分析对象（中文）`

指标清单 Excel（2行表头）:
- 第1行: 实际列名（指标名称、来源dws模型等）⭐
- 第2行: 子分类/详细说明
- 关键列: `指标名称`、`来源dws模型`、`业务定义及说明`

**推荐用法示例**:

```bash
# 1. 基础用法：从Excel生成（自动检测工作表）
python scripts/generate_business_config.py \
  --config=conf/agent.yml \
  --namespace=test \
  --arch-xlsx=/path/to/数据架构详细设计v2.3.xlsx \
  --metrics-xlsx=/path/to/打铁指标清单v2.4.xlsx \
  --verbose

# 2. 合并 DDL 注释（推荐）
python scripts/generate_business_config.py \
  --config=conf/agent.yml \
  --namespace=test \
  --arch-xlsx=/path/to/数据架构详细设计v2.3.xlsx \
  --metrics-xlsx=/path/to/打铁指标清单v2.4.xlsx \
  --merge-ddl \
  -v

# 3. 指定工作表名称
python scripts/generate_business_config.py \
  --config=conf/agent.yml \
  --namespace=test \
  --arch-xlsx=/path/to/数据架构详细设计v2.3.xlsx \
  --arch-sheet-name="模型设计" \
  --metrics-xlsx=/path/to/打铁指标清单v2.4.xlsx \
  --metrics-sheet-name=0

# 4. LLM 增强模式（术语消歧）
python scripts/generate_business_config.py \
  --config=conf/agent.yml \
  --namespace=test \
  --arch-xlsx=/path/to/数据架构详细设计v2.3.xlsx \
  --use-llm \
  --include-confidence \
  --output=conf/business_terms_debug.yml

# 5. 仅处理数据架构（不处理指标清单）
python scripts/generate_business_config.py \
  --config=conf/agent.yml \
  --namespace=test \
  --arch-xlsx=/path/to/数据架构详细设计v2.3.xlsx \
  --merge-ddl
```

**文本清洗功能**:

脚本自动对 Excel 单元格内容进行清洗，处理人类可读格式：
- **移除 emoji**: 🔥📊✅ 等
- **移除序号**: `1.`、`①`、`(1)`、`（一）` 等
- **规范化空格**: 多个空格/换行合并为单个
- **移除特殊符号**: `*#^~|\` 等

示例清洗效果:
| 原始文本 | 清洗后 |
|---------|--------|
| `1. 原始线索数\n这是详情` | `原始线索数 这是详情` |
| `① 首触线索数 (包含所有渠道)` | `首触线索数 (包含所有渠道)` |
| `✅ 有效线索数` | `有效线索数` |

使用 `--disable-text-cleaning` 可禁用此功能。

**表优先级过滤**:

脚本根据表名前缀自动过滤，优先级顺序:
| 优先级 | 前缀 | 说明 | 默认包含 |
|--------|------|------|----------|
| 1 | `dim_` | 维度表 | ✓ |
| 2 | `dwd_` | 明细事实表 | ✓ |
| 3 | `dws_` | 汇总事实表 | ✓ |
| 4 | `ads_` | 应用数据表 | ✓ |
| 5 | `ods_` | 操作数据表 | ✗ |

使用 `--max-table-priority=DWS` 可只包含 DIM/DWD/DWS 三层，排除 ADS/ODS。

**输出文件示例** (`conf/business_terms.yml`):
```yaml
# Business Terms Configuration
# Generated by generate_business_config.py
# Namespace: test
# Total mappings: 150 table terms, 320 schema terms

term_to_table:
  有效线索: ["dws_clue_valid", "dwd_clue_base"]
  试驾预约: ["dws_test_drive", "dwd_test_drive_base"]
  首触渠道: ["dws_channel_first_touch"]
  
term_to_schema:
  客户姓名: ["customer_name", "dwd_customer.customer_name"]
  手机号码: ["mobile_phone", "dwd_clue_base.mobile_phone"]
  
table_keywords:
  首触: "dws_channel_first_touch"
  有效线索: "dws_clue_valid"
  铂智3X: "dim_vehicle_model"
```

**配置加载规则**:
生成后的 `business_terms.yml` 从 agent.yml 所在目录加载:
1. 如果 `./conf/agent.yml` 存在，则从 `./conf/business_terms.yml` 加载
2. 否则从 `~/.datus/conf/business_terms.yml` 加载

> 注意: 已移除环境变量 `BUSINESS_TERM_CONFIG` 支持，简化配置管理。

**故障排除**:

| 问题 | 解决方案 |
|------|----------|
| "0/5559 有效行" | 检查 `--arch-sheet-name` 是否正确，确认Excel表头行数 |
| 列名匹配失败 | 使用 `-vv` 查看实际列名，检查Excel格式 |
| 找不到工作表 | 使用 `pd.ExcelFile(xlsx).sheet_names` 查看可用工作表 |
| 内存不足 | 分批处理或使用 CSV 格式 |
| ODS 表过多 | 使用 `--max-table-priority=ADS` 或 `DWS` 过滤 |
| 术语包含乱码/emoji | 文本清洗自动处理，或使用 `--disable-text-cleaning` 禁用 |

---

### 1.10 enrich_schema_from_design.py - Schema 元数据增强

**功能**: 从数据架构设计文档（Excel）直接增强 LanceDB 中的 Schema 元数据，支持数据层过滤和 LLM 智能匹配。

**与 generate_business_config.py 的区别**:
- `generate_business_config.py`: 生成业务术语配置文件（用于 text2sql 的术语映射）
- `enrich_schema_from_design.py`: 直接修改 LanceDB 中的 schema 元数据（提高元数据质量）

**核心特性**:
- **数据层过滤**: 支持 `--layers=dwd,dws,dim` 选择性处理特定数据层
- **智能匹配**: 精确匹配 + 模糊匹配 + LLM 语义匹配
- **匹配追踪**: 输出匹配来源统计（exact/fuzzy/none）
- **LLM 增强**: 智能匹配模糊表名、自动生成缺失注释

**核心增强内容**:
| 元数据字段 | 增强来源 | 说明 |
|------------|----------|------|
| `table_comment` | 逻辑实体业务含义 | 表的业务描述 |
| `column_comments` | 属性业务定义 + LLM生成 | 字段的详细业务含义 |
| `business_tags` | 主题域分组、分析对象 | 用于检索和分类 |
| `column_enums` | 字段名模式+安全分类 | 推断枚举值 |

**使用方式**:
```bash
python scripts/enrich_schema_from_design.py \
  --config=/root/.datus/conf/agent.yml \
  --namespace=test \
  --arch-xlsx=/path/to/数据架构详细设计v2.3.xlsx \
  --layers=dwd,dws,dim \
  [--dry-run | --apply]
```

**核心参数**:
| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--config` | 配置文件路径 | 必填 |
| `--namespace` | 目标命名空间 | 必填 |
| `--arch-xlsx` | 数据架构设计 Excel 路径 | 必填 |
| `--arch-sheet-name` | 工作表名称/索引 | 自动检测 |
| `--layers` | 目标数据层（逗号分隔） | `dwd,dws,dim` |
| `--dry-run` | 预览变更，不实际修改 | - |
| `--apply` | 应用变更到 LanceDB | - |
| `--use-llm` | 启用 LLM 智能匹配和注释生成 | false |
| `--output` | 保存报告路径 | - |
| `-v, --verbose` | 详细输出 | false |

**设计文档格式要点**:
- Excel **第2行**是实际列名（header=1）
- **`逻辑实体（英文）`列实际存储物理表名**（如 `dwd_assign_dlr_clue_fact_di`）
- 表名匹配以 `逻辑实体（英文）` 列为优先

**使用示例**:

```bash
# 1. 预览变更 - DWD/DWS/DIM 层（默认）
python scripts/enrich_schema_from_design.py \
  --config=/root/.datus/conf/agent.yml \
  --namespace=test \
  --arch-xlsx=/path/to/数据架构详细设计v2.3.xlsx \
  --layers=dwd,dws,dim \
  --dry-run \
  --output=/tmp/enrichment_preview.txt

# 2. 应用增强 - 只处理 DWS 层
python scripts/enrich_schema_from_design.py \
  --config=/root/.datus/conf/agent.yml \
  --namespace=test \
  --arch-xlsx=/path/to/数据架构详细设计v2.3.xlsx \
  --layers=dws \
  --apply

# 3. LLM 增强模式（智能匹配和注释生成）
python scripts/enrich_schema_from_design.py \
  --config=/root/.datus/conf/agent.yml \
  --namespace=test \
  --arch-xlsx=/path/to/数据架构详细设计v2.3.xlsx \
  --layers=dwd,dws,dim \
  --apply \
  --use-llm

# 4. 验证增强效果
python scripts/check_migration_report.py \
  --config=/root/.datus/conf/agent.yml \
  --namespace=test
```

---

### 1.11 verify_schema_enrichment.py - Schema 增强验证

**功能**: 验证 Schema 增强效果，对比增强前后的元数据质量变化，输出详细的改进报告。

**使用方式**:
```bash
python scripts/verify_schema_enrichment.py \
  --config=/root/.datus/conf/agent.yml \
  --namespace=test \
  [--before-backup=<path>]
```

**核心参数**:
| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--config` | 配置文件路径 | 必填 |
| `--namespace` | 命名空间 | 必填 |
| `--before-backup` | 增强前的备份数据库路径 | - |
| `--current-db` | 覆盖当前数据库路径 | - |
| `--output-json` | 保存详细结果到 JSON | - |

**使用示例**:

```bash
# 对比增强前后（需要备份）
python scripts/verify_schema_enrichment.py \
  --config=/root/.datus/conf/agent.yml \
  --namespace=test \
  --before-backup=/root/.datus/data/datus_db_test.backup_v0_20260128_120000 \
  --output-json=/tmp/enrichment_comparison.json

# 仅查看当前状态
python scripts/verify_schema_enrichment.py \
  --config=/root/.datus/conf/agent.yml \
  --namespace=test
```

**输出示例**:
```
================================================================================
SCHEMA ENRICHMENT VERIFICATION REPORT
================================================================================

Overall Score Comparison:
  Before: 45.2/100
  After:  78.6/100
  Improvement: +33.4

Coverage Changes:
  table_comment:
    Before: 120 tables (28.5%)
    After:  380 tables (90.2%)
  column_comments:
    Before: 85 tables (20.1%)
    After:  320 tables (75.9%)
  business_tags:
    Before: 95 tables (22.5%)
    After:  350 tables (83.0%)
```

---

## 二、模型下载

### 2.1 download_models_cn.sh - 国内模型下载

**功能**: 下载 HuggingFace 模型，支持国内镜像和 ModelScope 回退。

**使用方式**:
```bash
bash scripts/download_models_cn.sh
```

**特点**:
- 自动检测并切换 HuggingFace 镜像
- 支持 ModelScope 作为备选源
- 下载到 `~/.datus/models/`

**下载内容**:
1. `qdrant/all-MiniLM-L6-v2-onnx` - 核心嵌入模型（必需）
2. `BAAI/bge-reranker-large` - 重排序模型（可选）

**手动下载示例**:
```bash
# 仅下载嵌入模型
bash scripts/download_models_cn.sh
# 出现提示时按 N 跳过重排序模型
```

---

### 2.2 download_models_docker.sh - Docker 模型下载

**功能**: Docker 构建专用模型下载，下载到 `docker/huggingface/` 目录。

**使用方式**:
```bash
bash scripts/download_models_docker.sh
```

**用途**: 在构建 Docker 镜像前预下载模型，避免网络超时。

**下载内容**:
1. `qdrant/all-MiniLM-L6-v2-onnx` → `docker/huggingface/fastembed/`
2. `BAAI/bge-reranker-large` → `docker/huggingface/reranker/`

**Docker 构建流程**:
```bash
# 1. 下载模型
bash scripts/download_models_docker.sh

# 2. 构建镜像
docker build -t datus-agent:latest .

# 3. 运行容器
docker run -p 8080:8080 \
  -v /path/to/agent.yml:/root/.datus/conf/agent.yml \
  datus-agent:latest
```

---

## 三、开发测试

### 3.1 test_sql_review.sh - SQL 审查测试

**功能**: 发送 SQL Review 请求并保存响应日志。

**使用方式**:
```bash
bash scripts/test_sql_review.sh
```

**输出**:
- `test_output/sql_review_YYYYMMDD_HHMMSS.log` - 完整日志
- `test_output/sql_review_YYYYMMDD_HHMMSS_messages.txt` - SSE 消息流

**配置修改**:
编辑脚本开头的配置变量:
```bash
API_URL="http://localhost:8080/workflows/chat_research"
NAMESPACE="test"
CATALOG_NAME="default_catalog"
DATABASE_NAME="test"
```

---

### 3.2 update_prompt_templates.py - 提示模板更新

**功能**: 从 JSON 文件批量更新提示模板。

**使用方式**:
```bash
python scripts/update_prompt_templates.py --config=conf/agent.yml --input=prompts.json
```

**参数**:
| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--config` | 配置文件路径 | 必填 |
| `--input` | JSON 输入文件 | 必填 |
| `--dry-run` | 预览模式（不实际更新） | false |

**输入文件格式**:
```json
{
  "templates": [
    {
      "name": "gen_sql_user",
      "version": "1.1",
      "content": "# 新模板内容..."
    }
  ]
}
```

---

## 四、使用流程指南

### 4.1 完整 Schema 迁移流程（推荐）

```bash
# 1. 备份现有数据
bash scripts/backup_storage.sh

# 2. 执行迁移（包含统计信息、关系、LLM枚举提取）
python scripts/migrate_v0_to_v1.py \
  --config=conf/agent.yml \
  --namespace=test \
  --extract-statistics=true \
  --extract-relationships=true \
  --import-schemas \
  --llm-enum-extraction \
  --clear \
  --force

# 3. 【关键步骤】从设计文档增强 Schema 元数据（DWD/DWS/DIM 层）
python scripts/enrich_schema_from_design.py \
  --config=/root/.datus/conf/agent.yml \
  --namespace=test \
  --arch-xlsx=/path/to/数据架构详细设计v2.3.xlsx \
  --layers=dwd,dws,dim \
  --apply

# 4. 【可选】LLM 增强模式（智能匹配和注释生成）
python scripts/enrich_schema_from_design.py \
  --config=/root/.datus/conf/agent.yml \
  --namespace=test \
  --arch-xlsx=/path/to/数据架构详细设计v2.3.xlsx \
  --layers=dwd,dws,dim \
  --apply \
  --use-llm

# 5. 【可选】验证增强效果
python scripts/verify_schema_enrichment.py \
  --config=/root/.datus/conf/agent.yml \
  --namespace=test

# 5. 验证迁移质量
python scripts/check_migration_report.py \
  --config=conf/agent.yml \
  --namespace=test

# 6. 抽样诊断 DDL
python scripts/diagnose_schema_ddl.py \
  --config=conf/agent.yml \
  --namespace=test \
  --limit=10 \
  --random \
  --compare-db
```

**流程说明**:
1. `migrate_v0_to_v1.py`: 从数据库导入基础 Schema 结构
2. `enrich_schema_from_design.py`: 利用设计文档补充业务元数据（表注释、列注释、标签等）
3. 两者配合可大幅提升 text2sql 的 schema 理解能力

### 4.2 Docker 部署流程

```bash
# 1. 下载模型
bash scripts/download_models_docker.sh

# 2. 构建镜像
docker build -t datus-agent:latest .

# 3. 运行服务
docker run -p 8080:8080 \
  -v /path/to/agent.yml:/root/.datus/conf/agent.yml \
  datus-agent:latest
```

### 4.3 国内环境模型下载

```bash
# 使用国内镜像下载
bash scripts/download_models_cn.sh

# 模型会保存到 ~/.datus/models/
```

---

## 五、故障排除

### 5.1 常见问题

| 问题 | 解决方案 |
|------|----------|
| LLM 增强失败 | 禁用 LLM 增强模式，去掉 `--use-llm` 参数 |
| 模型下载超时 | 使用国内镜像：`bash scripts/download_models_cn.sh` |
| 权限错误 | 确保数据库用户有访问系统表权限 |
| 存储路径冲突 | 检查 `agent.yml` 中的 namespace 配置 |

### 5.2 回滚操作

```bash
# 从备份恢复
bash scripts/backup_storage.sh  # 先创建新备份
rm -rf ~/.datus/data/datus_db
mv ~/.datus/data/datus_db.backup_* ~/.datus/data/datus_db
```

---

## 版本更新记录

### v1.4 (2026-01-28)
- 重构 `generate_business_config.py` 脚本
  - **新增表优先级过滤**: `--max-table-priority` 参数支持 DIM/DWD/DWS/ADS/ODS 五级过滤
  - **新增智能文本清洗**: 自动去除 emoji、序号、特殊符号，可禁用
  - **新增 LLM 改写功能**: `--rewrite-with-llm` 参数支持指标定义智能改写
  - **简化配置加载**: 移除环境变量支持，直接从 agent.yml 所在目录加载
  - 改进关键词提取质量，限制长度 2-20 字符

### v1.3 (2026-01-28)
- 更新 `enrich_schema_from_design.py` 脚本文档
  - 新增 `--layers` 参数支持数据层过滤（默认 dwd,dws,dim）
  - 新增 `--use-llm` 参数支持 LLM 智能匹配和注释生成
  - 新增匹配来源追踪（exact/fuzzy/none）
  - 修复 LanceDB 加载数量问题
  - 明确 `逻辑实体（英文）` 列实际存储物理表名

### v1.2 (2026-01-28)
- 新增 `enrich_schema_from_design.py` 脚本文档
- 新增 `verify_schema_enrichment.py` 脚本文档
- 更新 Schema 迁移流程，包含设计文档增强步骤
- 添加 Schema 增强与业务术语生成的区别说明

### v1.1 (2026-01-28)
- 新增 `generate_business_config.py` 脚本完整文档
- 添加 Excel/CSV 输入格式说明
- 添加多行表头处理和工作表选择指南
- 添加 LLM 增强模式和故障排除

### v1.0 (2026-01-27)
- 初始版本
- 整合所有 scripts 目录脚本说明
- 新增脚本分类和快速索引
- 添加使用流程指南

---

## 相关资源

- **项目主页**: [https://datus.ai](https://datus.ai)
- **文档**: [https://docs.datus.ai](https://docs.datus.ai)
- **GitHub**: [https://github.com/Datus-ai/Datus-agent](https://github.com/Datus-ai/Datus-agent)
